#include<stdio.h>
int main()
{
    int l,b,A;
    printf("enter length and breadth of the rectangle to get area ");
    scanf("%d %d",&l, &b);
    A=l*b;
    printf("The Area of the rectangle is = %d ",A);
    return 0;
}