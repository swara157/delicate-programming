#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter any two numbers to multiply them  ");
    scanf("%d",&a );
    scanf("%d",&b );
    printf("The two number entered are %d %d\n",a,b);
     c=a*b;
    printf("The multiplication of two numbers is %d ", c);
    return 0;  
}